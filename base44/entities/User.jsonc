{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "owner",
        "user",
        "auditor",
        "system"
      ],
      "default": "user",
      "description": "User role: owner (admin), user, auditor, or system service account"
    }
  }
}